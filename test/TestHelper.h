#ifndef KALMAN_TEST_HELPER_H_
#define KALMAN_TEST_HELPER_H_

// Pulls in the lightweight, dependency-free test harness used across the
// Kalman unit tests. The harness provides the GoogleTest-compatible `TEST`
// macro, the `ASSERT_*` assertions and `ASSERT_MATRIX_NEAR`.
#include "lib/Test.hpp"

#endif
